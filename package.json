{
  "name": "borderline",
  "private": true,
  "description": "Dev-only tooling for the Borderline static site. The site itself has zero dependencies - this file exists only so `npm test` can run the Playwright regression suite in tests/.",
  "scripts": {
    "test": "node tests/run-all.js"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
